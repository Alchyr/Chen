package Chen.Interfaces;

public interface SpellCard {
    //perhaps add a function here that generates an orb
    SpellCard getCopyAsSpellCard(); //for shift cards with two forms, should return a card in the appropriate form
}
